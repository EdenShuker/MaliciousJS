extends layout
block head
    script(src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js")

block content
    div
        h1= title

    body
        ul
            il#myIl
        script.
            var pose = document.URL.indexOf("context=")+8;
            document.getElementById('myIl').innerHTML = document.URL.substring(pose, document.URL.length);
//            document.write(document.URL.substring(pose, document.URL.length));